# 프로젝트 구조 및 스크립트 역할 문서

이 문서는 프로젝트 내의 각 Python 스크립트의 역할과 상호 관계를 설명합니다.

## 📁 프로젝트 개요

이 프로젝트는 두 개의 주요 부분으로 구성됩니다:

1. **Speaking Practice Tool** (메인 앱) - 영어 말하기 연습을 위한 Flask 웹 애플리케이션
2. **YouTube Transcriber for Shadowing** - YouTube 비디오 전사 및 오디오 클리핑 도구

---

## 🎯 Part 1: Speaking Practice Tool (메인 앱)

### 핵심 애플리케이션 파일

#### `app.py` - Flask 웹 애플리케이션 메인
**역할**: 웹 서버의 진입점, 모든 API 엔드포인트 제공

**주요 기능**:
- Flask 서버 실행 및 라우팅
- RESTful API 엔드포인트 제공:
  - `/api/progress` - 진행 상황 조회/업데이트 (GET/POST)
  - `/api/roadmap` - Phase 1 로드맵 구조 조회
  - `/api/week/<week_key>` - 특정 주의 진행 상황 조회
  - `/api/activity-info` - 활동 정보 조회
  - `/api/generate/<activity_id>` - 특정 활동 콘텐츠 생성
  - `/api/generate-all` - 모든 콘텐츠 일괄 생성 (ChatGPT)
  - `/api/generate-audio-single` - Shadowing 스크립트 오디오 생성 (Typecast/OpenAI TTS)
  - `/api/generate-weekly-prompt-audio` - Weekly Prompt 오디오 생성
  - `/api/voices` - 사용 가능한 Typecast 음성 목록 조회
  - `/api/save-notes` - Weekly Prompt 노트 저장
  - `/api/save-recording` - 녹음 파일 저장
  - `/api/get-recordings` - 녹음 파일 목록 조회
  - `/api/delete-recording` - 녹음 파일 삭제
  - `/api/weekly-expressions/*` - Weekly Expressions 관련 API
  - `/api/podcast-shadowing/*` - 팟캐스트 섀도잉 관련 API
- 프론트엔드와 백엔드 연결

**의존성**:
- `progress_manager.py` - 진행 상황 관리
- `chatgpt_generator.py` - AI 콘텐츠 생성
- `typecast_generator.py` - TTS 오디오 생성

---

#### `config.py` - 설정 관리
**역할**: 환경 변수 및 설정값 관리

**주요 내용**:
- API 키 (OpenAI, Typecast)
- 파일 경로 설정
- Flask 서버 설정 (포트, 호스트, 디버그 모드)

**사용처**: 모든 모듈에서 환경 변수 접근

---

#### `progress_manager.py` - 진행 상황 관리
**역할**: 주간 진행 상황 추적 및 데이터 영속성 관리

**주요 기능**:
- `progress.json` 파일 읽기/쓰기
- 주간 키 계산 (예: "2025-W45")
- 진행 상황 업데이트 및 조회
- Phase 1 로드맵 구조 제공
- 이전 주 콘텐츠 조회

**사용처**:
- `app.py` - API에서 진행 상황 조회/업데이트
- 프론트엔드 - 진행 상황 표시

---

#### `chatgpt_generator.py` - ChatGPT 콘텐츠 생성
**역할**: OpenAI API를 사용한 AI 콘텐츠 생성

**주요 기능**:
- Voice Journaling 주제 생성 (주당 7개)
- Shadowing 스크립트 생성 (주당 2개)
- Weekly Speaking Prompt 생성
- Weekly Expressions 단어 생성 (주당 5개)
- OpenAI TTS로 오디오 생성 (선택적)

**의존성**:
- `config.py` - API 키
- `prompts.py` - 프롬프트 템플릿

**사용처**:
- `app.py` - `/api/generate-all` 엔드포인트

---

#### `typecast_generator.py` - Typecast.ai TTS 통합
**역할**: Typecast.ai API를 사용한 텍스트-음성 변환

**주요 기능**:
- 사용 가능한 음성 목록 조회
- 텍스트를 오디오로 변환
- 음성 및 속도 커스터마이징
- 오디오 파일 저장 및 관리
- **API 요청 로깅**: 모든 Typecast API 요청을 `typecast_requests.log`에 자동 기록
  - 요청 타임스탬프, 페이로드(음성 ID, 텍스트, 모델, 속도 등) 포함
  - 디버깅 및 API 사용량 추적에 활용

**의존성**:
- `config.py` - API 키

**사용처**:
- `app.py` - `/api/generate-audio-single`, `/api/generate-weekly-prompt-audio` 엔드포인트
- `app.py` - `/api/podcast-shadowing/generate-typecast-audio` 엔드포인트

**로그 파일**:
- `typecast_requests.log`: Typecast API 요청 로그 (자동 생성 및 관리)

---

#### `prompts.py` - ChatGPT 프롬프트 템플릿
**역할**: AI 생성에 사용되는 프롬프트 정의

**주요 내용**:
- 시스템 프롬프트 (배경 컨텍스트, 규칙)
- 사용자 프롬프트 템플릿
- 프롬프트 커스터마이징 가능

**사용처**:
- `chatgpt_generator.py` - 콘텐츠 생성 시 프롬프트 사용

**참고**: `prompts.py.example`을 복사하여 생성 (gitignored)

---

#### `send_reminder.py` - 이메일 리마인더 스크립트
**역할**: Gmail SMTP를 통한 연습 리마인더 이메일 전송

**주요 기능**:
- Gmail SMTP를 통한 이메일 전송
- 일일 연습 활동 리마인더
- Cron 작업으로 자동화 가능

**의존성**:
- `config.py` - Gmail 설정 (`.env` 파일)

**사용 방법**:
```bash
python3 send_reminder.py  # 수동 실행
# 또는 cron으로 자동화
```

---

## 🎬 Part 2: YouTube Transcriber for Shadowing

### 핵심 라이브러리 모듈 (`youtube_transcriber/`)

#### `youtube_transcriber/__init__.py` - 패키지 초기화
**역할**: 패키지 진입점, 주요 클래스 export

---

#### `youtube_transcriber/config.py` - 설정 클래스
**역할**: YouTube 전사 도구의 설정 관리

**주요 설정**:
- 기본 디렉토리 구조 (audio, clips, transcripts)
- Whisper 모델 설정
- 오디오 포맷 및 품질

**사용처**: 모든 YouTube 전사 관련 모듈

---

#### `youtube_transcriber/transcriber.py` - 오디오 전사 엔진
**역할**: Faster-Whisper를 사용한 오디오 전사

**주요 기능**:
- 오디오 파일을 텍스트로 변환
- 타임스탬프가 포함된 세그먼트 생성
- JSON 형식으로 전사 결과 저장

**의존성**:
- `config.py` - Whisper 모델 설정
- `faster_whisper` 라이브러리

**사용처**:
- `transcribe_chapter_clips.py`
- `process_playlist_chapters.py`
- `batch_transcribe.py`

---

#### `youtube_transcriber/playlist_handler.py` - YouTube 플레이리스트 처리
**역할**: YouTube 플레이리스트 및 비디오 정보 추출

**주요 기능**:
- 플레이리스트에서 비디오 목록 가져오기
- 비디오 메타데이터 추출 (제목, 길이, 업로드 날짜 등)
- 챕터 정보 추출
- 오디오 다운로드

**의존성**:
- `config.py` - 설정
- `yt_dlp` 라이브러리

**사용처**:
- `process_playlist_chapters.py`
- `transcribe_chapter_clips.py`

---

#### `youtube_transcriber/audio_clipper.py` - 오디오 클리핑
**역할**: 오디오 파일에서 특정 시간 구간 추출

**주요 기능**:
- FFmpeg를 사용한 오디오 클리핑
- 챕터별 오디오 클립 생성
- 비디오 오디오에서 클립 추출

**의존성**:
- `config.py` - 설정
- FFmpeg (시스템 도구)

**사용처**:
- `process_playlist_chapters.py`
- `generate_audio_clips.py`
- `generate_missing_clips.py`

---

#### `youtube_transcriber/transcript_searcher.py` - 전사본 검색
**역할**: 전사본에서 텍스트 검색 및 타임스탬프 찾기

**주요 기능**:
- 특정 텍스트가 포함된 세그먼트 찾기
- 시작 시간 찾기
- 시간 범위 찾기
- 주제 변경 감지 (키워드 분석)
- YouTube 챕터 기반 경계 찾기

**의존성**:
- `config.py` - 설정
- `playlist_handler.py` - 챕터 정보 (선택적)

**사용처**:
- `process_playlist_chapters.py`
- `tests/test_smart_clip.py`
- `tests/test_topic_clip.py`

---

#### `youtube_transcriber/transcript_formatter.py` - 전사본 포맷팅
**역할**: 타임스탬프 형식의 전사본을 자연스러운 스크립트 형식으로 변환

**주요 기능**:
- ChatGPT를 사용한 구두점 추가 및 대문자화
- 문단 구분
- 타임스탬프 주석 추가
- 반복 구문 정리

**의존성**:
- `config.py` - 설정
- `chatgpt_generator.py` (상위 디렉토리) - OpenAI 클라이언트

**사용처**:
- `format_existing_transcripts.py`
- `format_sample.py`

---

#### `youtube_transcriber/transcript_validator.py` - 전사본 검증
**역할**: 전사본 파일의 이상 및 오류 검사

**주요 기능**:
- 연속된 0 패턴 감지
- 깨진 문자 감지
- 의심스러운 숫자 패턴 감지
- 파일 읽기 오류 감지

**사용처**:
- `transcribe_chapter_clips.py` - 전사 후 검증
- 품질 관리 도구

---

#### `youtube_transcriber/utils.py` - 유틸리티 함수
**역할**: 공통 유틸리티 함수 제공

**주요 기능**:
- 로깅 설정
- 파일명 정리
- URL 파싱 (비디오 ID, 플레이리스트 ID 추출)

**사용처**: 모든 모듈에서 공통 기능 사용

---

### 실행 스크립트 (루트 디렉토리)

#### `process_playlist_chapters.py` - 플레이리스트 전체 처리
**역할**: YouTube 플레이리스트의 모든 비디오를 처리하는 메인 스크립트

**주요 기능**:
- 플레이리스트에서 비디오 목록 가져오기
- 각 비디오의 챕터 추출
- 챕터별 오디오 클립 생성
- 전사본 생성
- 메타데이터 저장

**사용 모듈**:
- `PlaylistHandler` - 비디오 정보 가져오기
- `AudioClipper` - 오디오 클리핑
- `Transcriber` - 전사
- `TranscriptSearcher` - 검색

**실행 방법**:
```python
from process_playlist_chapters import process_playlist_chapters
process_playlist_chapters(playlist_url, config=config)
```

---

#### `transcribe_chapter_clips.py` - 챕터 클립 전사
**역할**: 특정 비디오의 모든 챕터 오디오 클립을 전사

**주요 기능**:
- 비디오의 챕터 클립 찾기
- 각 클립을 Whisper로 전사
- 전사본 검증
- 포맷팅된 전사본 생성

**사용 모듈**:
- `Transcriber` - 전사
- `TranscriptValidator` - 검증

**실행 방법**:
```python
from transcribe_chapter_clips import transcribe_chapter_clips
transcribe_chapter_clips(video_id)
```

---

#### `transcribe_missing_chapters.py` - 누락된 챕터 전사
**역할**: 아직 전사되지 않은 챕터만 찾아서 전사

**주요 기능**:
- 메타데이터에서 챕터 목록 확인
- 전사본이 없는 챕터 찾기
- 누락된 챕터만 전사

**사용 모듈**:
- `Transcriber` - 전사

---

#### `format_existing_transcripts.py` - 기존 전사본 포맷팅
**역할**: 이미 생성된 전사본 파일들을 포맷팅

**주요 기능**:
- 디렉토리에서 전사본 파일 찾기
- 포맷팅되지 않은 파일만 처리
- ChatGPT로 자연스러운 형식으로 변환
- `_formatted.txt` 파일로 저장

**사용 모듈**:
- `transcript_formatter.py` - 포맷팅

**실행 방법**:
```bash
python3 format_existing_transcripts.py test_data/transcripts_large
```

---

#### `format_sample.py` - 샘플 파일 포맷팅
**역할**: 테스트용 샘플 파일 포맷팅

**주요 기능**:
- 특정 파일 1-2개를 포맷팅하여 결과 확인
- 개발/테스트용

**사용 모듈**:
- `transcript_formatter.py` - 포맷팅

---

#### `batch_transcribe.py` - 배치 전사
**역할**: 여러 비디오를 일괄 전사

**주요 기능**:
- 비디오 목록 읽기
- 각 비디오를 순차적으로 전사
- 진행 상황 로깅

**사용 모듈**:
- `Transcriber` - 전사

---

#### `generate_audio_clips.py` - 오디오 클립 생성
**역할**: 챕터 정보를 기반으로 오디오 클립 생성

**주요 기능**:
- 메타데이터에서 챕터 정보 읽기
- 각 챕터의 시간 범위로 오디오 클립 생성
- 클립 파일 저장

**사용 모듈**:
- `AudioClipper` - 오디오 클리핑

---

#### `generate_missing_clips.py` - 누락된 클립 생성
**역할**: 아직 생성되지 않은 오디오 클립만 생성

**주요 기능**:
- 메타데이터에서 챕터 목록 확인
- 클립 파일이 없는 챕터 찾기
- 누락된 클립만 생성

**사용 모듈**:
- `AudioClipper` - 오디오 클리핑

---

#### `collect_chapter_info.py` - 챕터 정보 수집
**역할**: YouTube 비디오에서 챕터 정보만 추출하여 저장

**주요 기능**:
- 비디오 URL에서 챕터 정보 가져오기
- 메타데이터 파일로 저장
- 오디오 다운로드나 전사 없이 정보만 수집

**사용 모듈**:
- `PlaylistHandler` - 챕터 정보 추출

---

#### `check_transcript_status.py` - 전사 상태 확인
**역할**: 전사본 파일의 상태 확인 및 보고

**주요 기능**:
- 전사본 파일 존재 여부 확인
- 메타데이터와 비교하여 누락 확인
- 상태 리포트 생성

---

#### `check_transcription_progress.py` - 전사 진행 상황 확인
**역할**: 배치 전사 작업의 진행 상황 확인

**주요 기능**:
- 완료된 전사본 수 확인
- 남은 작업 확인
- 진행률 계산

---

#### `compare_transcripts.py` - 전사본 비교
**역할**: 여러 전사본을 비교하여 차이점 분석

**주요 기능**:
- 두 전사본 비교
- 차이점 하이라이트
- 품질 분석

---

#### `transcribe_videos_1_3_large.py` - 대용량 비디오 전사 (1-3)
**역할**: 특정 비디오들(1-3)을 대용량 모델로 전사

**주요 기능**:
- Whisper large 모델 사용
- 긴 비디오 전사
- 고품질 전사본 생성

**사용 모듈**:
- `Transcriber` - large 모델로 전사

---

#### `transcribe_video_4_large.py` - 대용량 비디오 전사 (4)
**역할**: 특정 비디오(4)를 대용량 모델로 전사

**주요 기능**:
- Whisper large 모델 사용
- 단일 비디오 전사

**사용 모듈**:
- `Transcriber` - large 모델로 전사

---

#### `generate_large_formatted_list.py` - 대용량 포맷팅 리스트 생성
**역할**: 대용량 전사본 파일 목록 생성

**주요 기능**:
- `transcripts_large/` 디렉토리의 파일 목록 생성
- 포맷팅 작업을 위한 파일 목록 제공

---

### 테스트 스크립트 (`tests/`)

#### `tests/test_transcribe.py` - 전사 테스트
**역할**: 전사 기능 단위 테스트

---

#### `tests/test_clip.py` - 클리핑 테스트
**역할**: 오디오 클리핑 기능 테스트

---

#### `tests/test_playlist.py` - 플레이리스트 테스트
**역할**: 플레이리스트 처리 기능 테스트

---

#### `tests/test_smart_clip.py` - 스마트 클리핑 테스트
**역할**: 텍스트 기반 스마트 클리핑 테스트

**주요 기능**:
- 텍스트 검색으로 시작 시간 찾기
- 시간 범위로 클립 생성
- `TranscriptSearcher` 사용 예시

---

#### `tests/test_topic_clip.py` - 주제 기반 클리핑 테스트
**역할**: 주제 변경 감지를 사용한 클리핑 테스트

**주요 기능**:
- 주제 변경 감지 테스트
- YouTube 챕터 사용 테스트
- `TranscriptSearcher.detect_topic_change()` 사용 예시

---

#### `tests/test_download.py` - 다운로드 테스트
**역할**: YouTube 오디오 다운로드 테스트

---

## 🔗 모듈 간 의존성 관계

### Speaking Practice Tool 의존성 그래프

```
app.py
├── progress_manager.py
│   └── config.py
├── chatgpt_generator.py
│   ├── config.py
│   └── prompts.py
└── typecast_generator.py
    └── config.py
```

### YouTube Transcriber 의존성 그래프

```
process_playlist_chapters.py
├── PlaylistHandler (playlist_handler.py)
│   ├── Config (config.py)
│   └── utils.py
├── AudioClipper (audio_clipper.py)
│   └── Config (config.py)
├── Transcriber (transcriber.py)
│   └── Config (config.py)
└── TranscriptSearcher (transcript_searcher.py)
    ├── Config (config.py)
    └── PlaylistHandler (playlist_handler.py)

transcribe_chapter_clips.py
├── Transcriber (transcriber.py)
│   └── Config (config.py)
└── TranscriptValidator (transcript_validator.py)

format_existing_transcripts.py
└── transcript_formatter.py
    └── chatgpt_generator.py (상위 디렉토리)
        └── config.py (상위 디렉토리)
```

## 📊 데이터 흐름

### Speaking Practice Tool 워크플로우

1. **콘텐츠 생성**:
   ```
   사용자 요청 → app.py → chatgpt_generator.py → OpenAI API
   → progress.json 저장 → 프론트엔드 표시
   ```

2. **오디오 생성**:
   ```
   사용자 요청 → app.py → typecast_generator.py → Typecast API
   → typecast_requests.log에 요청 기록 → 오디오 파일 저장 → 프론트엔드 재생
   ```

3. **진행 상황 업데이트**:
   ```
   사용자 체크박스 클릭 → app.py → progress_manager.py
   → progress.json 업데이트 → 프론트엔드 반영
   ```

### YouTube Transcriber 워크플로우

1. **플레이리스트 처리**:
   ```
   process_playlist_chapters.py
   → PlaylistHandler: 비디오 목록 가져오기
   → PlaylistHandler: 챕터 정보 추출
   → AudioClipper: 오디오 클립 생성
   → Transcriber: 클립 전사
   → JSON/TXT 파일 저장
   ```

2. **전사본 포맷팅**:
   ```
   format_existing_transcripts.py
   → transcript_formatter.py
   → ChatGPT API: 포맷팅
   → _formatted.txt 파일 저장
   ```

3. **스마트 클리핑**:
   ```
   TranscriptSearcher: 텍스트 검색
   → 시작 시간 찾기
   → 주제 변경 감지
   → AudioClipper: 클립 생성
   ```

## 🚀 주요 사용 시나리오

### 시나리오 1: 새로운 플레이리스트 처리
```bash
# 1. 플레이리스트 처리
python3 process_playlist_chapters.py

# 2. 챕터 클립 전사
python3 transcribe_chapter_clips.py <video_id>

# 3. 전사본 포맷팅
python3 format_existing_transcripts.py test_data/transcripts_large
```

### 시나리오 2: 웹 앱 실행
```bash
# 1. 환경 변수 설정 (.env 파일)
# 2. Flask 앱 실행
python3 app.py

# 3. 브라우저에서 http://localhost:5001 접속
```

### 시나리오 3: 누락된 작업 완료
```bash
# 1. 누락된 클립 생성
python3 generate_missing_clips.py

# 2. 누락된 전사본 생성
python3 transcribe_missing_chapters.py
```

## 📝 파일 구조 요약

```
speaking_practice_tool/
├── app.py                    # Flask 웹 서버
├── config.py                 # 설정 관리
├── progress_manager.py       # 진행 상황 관리
├── chatgpt_generator.py      # AI 콘텐츠 생성
├── typecast_generator.py     # TTS 오디오 생성
├── prompts.py                # 프롬프트 템플릿
├── send_reminder.py          # 이메일 리마인더
│
└── youtube-transcriber-for-shadowing/
    ├── youtube_transcriber/   # 핵심 라이브러리
    │   ├── config.py
    │   ├── transcriber.py
    │   ├── playlist_handler.py
    │   ├── audio_clipper.py
    │   ├── transcript_searcher.py
    │   ├── transcript_formatter.py
    │   ├── transcript_validator.py
    │   └── utils.py
    │
    ├── process_playlist_chapters.py    # 메인 처리 스크립트
    ├── transcribe_chapter_clips.py     # 챕터 전사
    ├── format_existing_transcripts.py  # 포맷팅
    ├── batch_transcribe.py              # 배치 전사
    └── tests/                           # 테스트 스크립트
```

## 🔍 주요 개념

### 1. Config 패턴
- 두 개의 독립적인 `config.py` 파일
- 각각 자신의 도메인 설정 관리
- 환경 변수 기반 설정

### 2. 모듈화 설계
- YouTube Transcriber는 재사용 가능한 클래스 기반 설계
- 각 클래스는 단일 책임 원칙 준수
- 스크립트는 클래스를 조합하여 사용

### 3. 데이터 저장
- `progress.json`: 웹 앱 진행 상황
- `playlist_metadata.json`: YouTube 비디오 메타데이터 (제목, URL, duration, upload_date, chapters 포함)
- `formatted_chapters_list.json`: 대용량 모델로 전사된 비디오의 챕터 목록 및 전사본 경로
- `*_metadata.json`: 개별 비디오 메타데이터 (선택적, 주로 `playlist_metadata.json` 사용)
- `*.json`: Whisper 전사본 (세그먼트 포함)
- `*.txt`: 포맷팅된 전사본

### 4. API 통합
- OpenAI API: 콘텐츠 생성 및 TTS
- Typecast.ai API: 고품질 TTS (요청 로깅 포함)
- YouTube API (yt_dlp): 비디오 정보 추출

### 5. 로그 파일 관리
- `typecast_requests.log`: Typecast API 요청 로그 (자동 생성, 활발히 사용 중)
  - 모든 TTS 요청의 페이로드와 타임스탬프 기록
  - 디버깅 및 API 사용량 추적에 활용
- `server.log`: Flask 서버 로그
- `reminder.log`: 이메일 리마인더 로그
- 기타 로그 파일: 수동 스크립트 실행 시 리다이렉션으로 생성되는 임시 로그 (일반적으로 삭제됨)

---

**마지막 업데이트**: 2025-01-16
**작성자**: 프로젝트 분석

